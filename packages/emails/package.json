{
  "name": "@hashpass/emails",
  "version": "1.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "generate": "node scripts/generate-templates.mjs",
    "build": "node scripts/generate-templates.mjs && tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
