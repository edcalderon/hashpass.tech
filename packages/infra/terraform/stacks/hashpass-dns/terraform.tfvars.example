# Target hosted zones for the HashPass AWS account migration
aws_region = "us-east-2"

tech_zone_name = "hashpass.tech"
lat_zone_name  = "hashpass.lat"
club_zone_name = "hashpass.club"

tags = {
  Environment = "target"
  Project     = "hashpass"
}
