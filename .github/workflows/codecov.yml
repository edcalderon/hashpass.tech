name: Coverage

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  coverage:
    runs-on: ubuntu-latest
    env:
      CI: '1'
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22.22.0"
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --prefer-offline

      - name: Run coverage tests
        run: pnpm --dir apps/mobile-app test:coverage

      - name: Run event ingestion coverage tests
        run: pnpm --filter @hashpass/event-ingestion test:coverage

      # These three packages weren't in any workflow at all -- their tests
      # only ever ran locally, so a passing local run gave no real CI
      # guarantee. No lcov/coverage reporting here (their tsx/node --test
      # runners don't produce it without extra tooling); this is just the
      # pass/fail gate.
      - name: Run backend tests
        run: pnpm --filter @hashpass/backend run test

      - name: Run hashpass-links-api tests
        run: pnpm --filter @hashpass/hashpass-links-api run test

      - name: Run sdk tests
        run: pnpm --filter @hashpass/sdk run test

      - name: Upload coverage to Codecov
        if: ${{ env.CODECOV_TOKEN != '' }}
        uses: codecov/codecov-action@v7
        with:
          files: ./apps/mobile-app/coverage/lcov.info,./packages/event-ingestion/coverage/lcov.info
          # The coverage test command above remains the required gate. Upload
          # failures are external/transient (and push + PR runs can upload the
          # same report concurrently), so they must not block a valid release.
          fail_ci_if_error: false
          verbose: true
          version: v11.2.8
          token: ${{ env.CODECOV_TOKEN }}
