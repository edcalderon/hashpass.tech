diff --git a/Libraries/Renderer/implementations/ReactFabric-dev.js b/Libraries/Renderer/implementations/ReactFabric-dev.js
index 153ddfa429f721716c86bfb4325396072644f3ce..acc561988999e0ff858ad80d6b5a3c0555005da8 100644
--- a/Libraries/Renderer/implementations/ReactFabric-dev.js
+++ b/Libraries/Renderer/implementations/ReactFabric-dev.js
@@ -14398,6 +14398,21 @@ __DEV__ &&
       customDirectEventTypes =
         ReactNativePrivateInterface.ReactNativeViewConfigRegistry
           .customDirectEventTypes;
+    function getHashPassDirectEventConfig(topLevelType) {
+      if (
+        "string" === typeof topLevelType &&
+        0 === topLevelType.indexOf("top") &&
+        3 < topLevelType.length
+      ) {
+        var directDispatchConfig = customDirectEventTypes[topLevelType];
+        null == directDispatchConfig &&
+          (directDispatchConfig = customDirectEventTypes[topLevelType] = {
+            registrationName: "on" + topLevelType.slice(3)
+          });
+        return directDispatchConfig;
+      }
+      return null;
+    }
     if (eventPluginOrder)
       throw Error(
         "EventPluginRegistry: Cannot inject event plugin ordering more than once. You are likely trying to load more than one copy of React."
@@ -14440,12 +14455,15 @@ __DEV__ &&
           if (null == targetInst) return null;
           var bubbleDispatchConfig = customBubblingEventTypes[topLevelType],
             directDispatchConfig = customDirectEventTypes[topLevelType];
-          if (!bubbleDispatchConfig && !directDispatchConfig)
-            throw Error(
-              'Unsupported top level event type "' +
-                topLevelType +
-                '" dispatched'
-            );
+          if (!bubbleDispatchConfig && !directDispatchConfig) {
+            directDispatchConfig = getHashPassDirectEventConfig(topLevelType);
+            if (!directDispatchConfig)
+              throw Error(
+                'Unsupported top level event type "' +
+                  topLevelType +
+                  '" dispatched'
+              );
+          }
           topLevelType = SyntheticEvent.getPooled(
             bubbleDispatchConfig || directDispatchConfig,
             targetInst,
diff --git a/Libraries/Renderer/implementations/ReactFabric-prod.js b/Libraries/Renderer/implementations/ReactFabric-prod.js
index 2c9abc9e887dfdabafca942c4ac8ec3c516fd6bb..d64cfff892d40cfc6f880bd402cc65ee422ff9bd 100644
--- a/Libraries/Renderer/implementations/ReactFabric-prod.js
+++ b/Libraries/Renderer/implementations/ReactFabric-prod.js
@@ -1100,6 +1100,21 @@ var customBubblingEventTypes =
   customDirectEventTypes =
     ReactNativePrivateInterface.ReactNativeViewConfigRegistry
       .customDirectEventTypes;
+function getHashPassDirectEventConfig(topLevelType) {
+  if (
+    "string" === typeof topLevelType &&
+    0 === topLevelType.indexOf("top") &&
+    3 < topLevelType.length
+  ) {
+    var directDispatchConfig = customDirectEventTypes[topLevelType];
+    null == directDispatchConfig &&
+      (directDispatchConfig = customDirectEventTypes[topLevelType] = {
+        registrationName: "on" + topLevelType.slice(3)
+      });
+    return directDispatchConfig;
+  }
+  return null;
+}
 function accumulateDirectionalDispatches(inst, phase, event) {
   if (
     (phase = getListener(
@@ -1177,10 +1192,15 @@ var injectedNamesToPlugins$jscomp$inline_281 = {
         if (null == targetInst) return null;
         var bubbleDispatchConfig = customBubblingEventTypes[topLevelType],
           directDispatchConfig = customDirectEventTypes[topLevelType];
-        if (!bubbleDispatchConfig && !directDispatchConfig)
-          throw Error(
-            'Unsupported top level event type "' + topLevelType + '" dispatched'
-          );
+        if (!bubbleDispatchConfig && !directDispatchConfig) {
+          directDispatchConfig = getHashPassDirectEventConfig(topLevelType);
+          if (!directDispatchConfig)
+            throw Error(
+              'Unsupported top level event type "' +
+                topLevelType +
+                '" dispatched'
+            );
+        }
         topLevelType = SyntheticEvent.getPooled(
           bubbleDispatchConfig || directDispatchConfig,
           targetInst,
diff --git a/Libraries/Renderer/implementations/ReactFabric-profiling.js b/Libraries/Renderer/implementations/ReactFabric-profiling.js
index c4b05f29d21cf4ac900f3dc4da796997cc02fa0a..8639e1028a734d89f656ab5536ce7fc19e8857f5 100644
--- a/Libraries/Renderer/implementations/ReactFabric-profiling.js
+++ b/Libraries/Renderer/implementations/ReactFabric-profiling.js
@@ -1104,6 +1104,21 @@ var customBubblingEventTypes =
   customDirectEventTypes =
     ReactNativePrivateInterface.ReactNativeViewConfigRegistry
       .customDirectEventTypes;
+function getHashPassDirectEventConfig(topLevelType) {
+  if (
+    "string" === typeof topLevelType &&
+    0 === topLevelType.indexOf("top") &&
+    3 < topLevelType.length
+  ) {
+    var directDispatchConfig = customDirectEventTypes[topLevelType];
+    null == directDispatchConfig &&
+      (directDispatchConfig = customDirectEventTypes[topLevelType] = {
+        registrationName: "on" + topLevelType.slice(3)
+      });
+    return directDispatchConfig;
+  }
+  return null;
+}
 function accumulateDirectionalDispatches(inst, phase, event) {
   if (
     (phase = getListener(
@@ -1181,10 +1196,15 @@ var injectedNamesToPlugins$jscomp$inline_297 = {
         if (null == targetInst) return null;
         var bubbleDispatchConfig = customBubblingEventTypes[topLevelType],
           directDispatchConfig = customDirectEventTypes[topLevelType];
-        if (!bubbleDispatchConfig && !directDispatchConfig)
-          throw Error(
-            'Unsupported top level event type "' + topLevelType + '" dispatched'
-          );
+        if (!bubbleDispatchConfig && !directDispatchConfig) {
+          directDispatchConfig = getHashPassDirectEventConfig(topLevelType);
+          if (!directDispatchConfig)
+            throw Error(
+              'Unsupported top level event type "' +
+                topLevelType +
+                '" dispatched'
+            );
+        }
         topLevelType = SyntheticEvent.getPooled(
           bubbleDispatchConfig || directDispatchConfig,
           targetInst,
diff --git a/Libraries/Renderer/implementations/ReactNativeRenderer-dev.js b/Libraries/Renderer/implementations/ReactNativeRenderer-dev.js
index e5f459021d69e4a67084de8f82e01a3a11e5ac3e..c5b939d6c31d6332df39a5f32be481252683cb47 100644
--- a/Libraries/Renderer/implementations/ReactNativeRenderer-dev.js
+++ b/Libraries/Renderer/implementations/ReactNativeRenderer-dev.js
@@ -14600,6 +14600,21 @@ __DEV__ &&
       customDirectEventTypes =
         ReactNativePrivateInterface.ReactNativeViewConfigRegistry
           .customDirectEventTypes;
+    function getHashPassDirectEventConfig(topLevelType) {
+      if (
+        "string" === typeof topLevelType &&
+        0 === topLevelType.indexOf("top") &&
+        3 < topLevelType.length
+      ) {
+        var directDispatchConfig = customDirectEventTypes[topLevelType];
+        null == directDispatchConfig &&
+          (directDispatchConfig = customDirectEventTypes[topLevelType] = {
+            registrationName: "on" + topLevelType.slice(3)
+          });
+        return directDispatchConfig;
+      }
+      return null;
+    }
     if (eventPluginOrder)
       throw Error(
         "EventPluginRegistry: Cannot inject event plugin ordering more than once. You are likely trying to load more than one copy of React."
@@ -14642,12 +14657,15 @@ __DEV__ &&
           if (null == targetInst) return null;
           var bubbleDispatchConfig = customBubblingEventTypes[topLevelType],
             directDispatchConfig = customDirectEventTypes[topLevelType];
-          if (!bubbleDispatchConfig && !directDispatchConfig)
-            throw Error(
-              'Unsupported top level event type "' +
-                topLevelType +
-                '" dispatched'
-            );
+          if (!bubbleDispatchConfig && !directDispatchConfig) {
+            directDispatchConfig = getHashPassDirectEventConfig(topLevelType);
+            if (!directDispatchConfig)
+              throw Error(
+                'Unsupported top level event type "' +
+                  topLevelType +
+                  '" dispatched'
+              );
+          }
           topLevelType = SyntheticEvent.getPooled(
             bubbleDispatchConfig || directDispatchConfig,
             targetInst,
diff --git a/Libraries/Renderer/implementations/ReactNativeRenderer-prod.js b/Libraries/Renderer/implementations/ReactNativeRenderer-prod.js
index cb3bb601531cdd7f5474fb34722e8784e651d7b4..5ae5c46ea7554cb93f04721ad27b558cdf05b349 100644
--- a/Libraries/Renderer/implementations/ReactNativeRenderer-prod.js
+++ b/Libraries/Renderer/implementations/ReactNativeRenderer-prod.js
@@ -1100,6 +1100,21 @@ var customBubblingEventTypes =
   customDirectEventTypes =
     ReactNativePrivateInterface.ReactNativeViewConfigRegistry
       .customDirectEventTypes;
+function getHashPassDirectEventConfig(topLevelType) {
+  if (
+    "string" === typeof topLevelType &&
+    0 === topLevelType.indexOf("top") &&
+    3 < topLevelType.length
+  ) {
+    var directDispatchConfig = customDirectEventTypes[topLevelType];
+    null == directDispatchConfig &&
+      (directDispatchConfig = customDirectEventTypes[topLevelType] = {
+        registrationName: "on" + topLevelType.slice(3)
+      });
+    return directDispatchConfig;
+  }
+  return null;
+}
 function accumulateDirectionalDispatches(inst, phase, event) {
   if (
     (phase = getListener(
@@ -1177,10 +1192,15 @@ var injectedNamesToPlugins$jscomp$inline_288 = {
         if (null == targetInst) return null;
         var bubbleDispatchConfig = customBubblingEventTypes[topLevelType],
           directDispatchConfig = customDirectEventTypes[topLevelType];
-        if (!bubbleDispatchConfig && !directDispatchConfig)
-          throw Error(
-            'Unsupported top level event type "' + topLevelType + '" dispatched'
-          );
+        if (!bubbleDispatchConfig && !directDispatchConfig) {
+          directDispatchConfig = getHashPassDirectEventConfig(topLevelType);
+          if (!directDispatchConfig)
+            throw Error(
+              'Unsupported top level event type "' +
+                topLevelType +
+                '" dispatched'
+            );
+        }
         topLevelType = SyntheticEvent.getPooled(
           bubbleDispatchConfig || directDispatchConfig,
           targetInst,
diff --git a/Libraries/Renderer/implementations/ReactNativeRenderer-profiling.js b/Libraries/Renderer/implementations/ReactNativeRenderer-profiling.js
index 505a2e44f4c9cf8f0a02decc6cf5570968a7e4f6..4a81f47256ba8f9c541ab780708bb21915e82ee6 100644
--- a/Libraries/Renderer/implementations/ReactNativeRenderer-profiling.js
+++ b/Libraries/Renderer/implementations/ReactNativeRenderer-profiling.js
@@ -1104,6 +1104,21 @@ var customBubblingEventTypes =
   customDirectEventTypes =
     ReactNativePrivateInterface.ReactNativeViewConfigRegistry
       .customDirectEventTypes;
+function getHashPassDirectEventConfig(topLevelType) {
+  if (
+    "string" === typeof topLevelType &&
+    0 === topLevelType.indexOf("top") &&
+    3 < topLevelType.length
+  ) {
+    var directDispatchConfig = customDirectEventTypes[topLevelType];
+    null == directDispatchConfig &&
+      (directDispatchConfig = customDirectEventTypes[topLevelType] = {
+        registrationName: "on" + topLevelType.slice(3)
+      });
+    return directDispatchConfig;
+  }
+  return null;
+}
 function accumulateDirectionalDispatches(inst, phase, event) {
   if (
     (phase = getListener(
@@ -1181,10 +1196,15 @@ var injectedNamesToPlugins$jscomp$inline_304 = {
         if (null == targetInst) return null;
         var bubbleDispatchConfig = customBubblingEventTypes[topLevelType],
           directDispatchConfig = customDirectEventTypes[topLevelType];
-        if (!bubbleDispatchConfig && !directDispatchConfig)
-          throw Error(
-            'Unsupported top level event type "' + topLevelType + '" dispatched'
-          );
+        if (!bubbleDispatchConfig && !directDispatchConfig) {
+          directDispatchConfig = getHashPassDirectEventConfig(topLevelType);
+          if (!directDispatchConfig)
+            throw Error(
+              'Unsupported top level event type "' +
+                topLevelType +
+                '" dispatched'
+            );
+        }
         topLevelType = SyntheticEvent.getPooled(
           bubbleDispatchConfig || directDispatchConfig,
           targetInst,
