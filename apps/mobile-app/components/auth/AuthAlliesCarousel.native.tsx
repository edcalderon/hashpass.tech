import type React from "react";

// The allies carousel belongs to the web-only desktop hero panel.
export default function AuthAlliesCarousel(_props: {
  children: React.ReactNode[];
  enabled: boolean;
  pauseLabel: string;
  resumeLabel: string;
  color: string;
}) {
  return null;
}
